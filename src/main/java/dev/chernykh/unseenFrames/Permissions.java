package dev.chernykh.unseenFrames;

public final class Permissions {

    /** Hide and reveal frames with the tool. */
    public static final String USE = "unseenframes.use";

    /** The /unseenframes command. */
    public static final String ADMIN = "unseenframes.admin";

    private Permissions() {
    }
}
